{
  "name": "frontend",
  "version": "1.0.0",
  "private": true,
  "description": "SPA: esbuild bundles src/app.js (+ Leaflet, fetch-event-source) into dist/app.js; the Tailwind v4 CLI compiles src/styles.css into dist/styles.css.",
  "scripts": {
    "build": "node build.mjs && tailwindcss -i src/styles.css -o dist/styles.css --minify"
  },
  "dependencies": {
    "@microsoft/fetch-event-source": "^2.0.1",
    "leaflet": "^1.9.4"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "esbuild": "^0.24.0",
    "tailwindcss": "^4.0.0"
  }
}
