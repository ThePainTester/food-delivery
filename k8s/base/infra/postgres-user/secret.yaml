# Demo credentials — overlay-overridable via secretGenerator if needed.
apiVersion: v1
kind: Secret
metadata:
  name: users-db-credentials
type: Opaque
stringData:
  username: users
  password: changeme
