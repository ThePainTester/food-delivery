# Template only — do not apply this file directly. The real Secret lives at
# `secret.yaml` (gitignored) and is generated per-cluster by
# `scripts/app-env-bootstrap.sh`. Keep this template in sync with the script.
apiVersion: v1
kind: Secret
metadata:
  name: users-db-credentials
type: Opaque
stringData:
  username: users
  password: REPLACE_ME
