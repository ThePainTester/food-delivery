apiVersion: v1
kind: Service
metadata:
  name: dispatch-cache
  labels:
    app: dispatch-cache
spec:
  selector:
    app: dispatch-cache
  ports:
    - name: redis
      port: 6379
      targetPort: redis
    - name: metrics
      port: 9121
      targetPort: metrics
