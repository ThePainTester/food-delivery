apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - secret.yaml
  - service.yaml
  - statefulset.yaml

# Schema is applied by an `initContainers:` block on the order-service
# Deployment that runs `migrate` against this DB before the app starts.
