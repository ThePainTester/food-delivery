apiVersion: v1
kind: Service
metadata:
  name: orders-db
  labels:
    app: orders-db
spec:
  clusterIP: None
  selector:
    app: orders-db
  ports:
    - name: pg
      port: 5432
      targetPort: pg
    - name: metrics
      port: 9187
      targetPort: metrics
