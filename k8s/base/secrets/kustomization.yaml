apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# jwt-privkey.yaml is generated per-cluster by scripts/app-env-bootstrap.sh
# and gitignored. See jwt-privkey.example.yaml for the template.
resources:
  - jwt-privkey.yaml
