apiVersion: apps/v1
kind: Deployment
metadata:
  name: _placeholder  # ignored — kustomization.yaml uses `target:` to select all Deployments
spec:
  template:
    spec:
      imagePullSecrets:
        - name: ghcr-credentials
